<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zgłoszenia - Sekcja <%= section %>
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .table-container {
            margin-top: 20px;
            padding: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        table,
        th,
        td {
            border: 1px solid #ddd;
        }

        th,
        td {
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }
    </style>
</head>

<body>
    <div class="table-container">
        <h3>Zgłoszenia Sekcja <%= section %>
        </h3>
        <table>
            <tr>
                <th>ID</th>
                <th>Kategoria</th>
            </tr>
            <% orders.forEach(order=> { %>
                <tr>
                    <td>
                        <%= order.section %>
                    </td>
                    <td>
                        <%= order.category %>
                    </td>
                </tr>
                <% }) %>
        </table>
    </div>
</body>

</html>